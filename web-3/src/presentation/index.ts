/**
 * Presentation Layer
 * 
 * This layer contains all UI components, hooks, and presentation logic.
 * It should only depend on the application layer, never directly on infrastructure.
 * 
 * Owner: FE (Frontend Agent)
 */

// Export all presentation services
export * from './services';

// Export all UI components
export * from './components/ui';

// Export all form components
export * from './components/forms';

// Export all apartment components
export * from './components/apartment';

// Export all shared hooks
export * from './hooks';