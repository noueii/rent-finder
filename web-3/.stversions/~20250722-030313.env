# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Next Auth
# You can generate a new secret on the command line with:
# npx auth secret
# https://next-auth.js.org/configuration/options#secret
AUTH_SECRET="development-secret-change-in-production"

# Next Auth Discord Provider
AUTH_DISCORD_ID="dummy-discord-id"
AUTH_DISCORD_SECRET="dummy-discord-secret"

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
DATABASE_URL="postgresql://postgres:password@localhost:5432/web-3"
BYPASS_ROBOTS_TXT="true"
NODE_ENV=development

ENABLE_PERFORMANCE_MONITORING="true"
PERFORMANCE_REPORT_INTERVAL="3600000"

PROXY_ROTATION_STRATEGY="performance" # round-robin, random, performance, least-used
PROXY_BLACKLIST_DURATION="60000" # 1 minute in ms (faster recovery)
PROXY_MAX_FAILURES="2" # Failures before blacklisting (quick detection)

# Fast Scraper Configuration
# Enable fast scrapers for 15x performance improvement
USE_FAST_SCRAPERS="true"
ENABLE_FAST_MODE="true"
DISABLE_SCRAPERS_PROXY="true"
PROXY_FILE="src/lib/scrapers/data/fast-socks-proxies.txt"

# HTTP Proxy Configuration for Wagaya and YOLO
# Override default SOCKS5 proxies with HTTP proxies
FASTWAGAYAJAPAN_PROXY_TYPE="http"
FASTWAGAYAJAPAN_PROXY_FILE="src/lib/scrapers/data/proxyscrape_premium_http_proxies.txt"
FASTYOLOJAPAN_PROXY_TYPE="http"  
FASTYOLOJAPAN_PROXY_FILE="src/lib/scrapers/data/proxyscrape_premium_http_proxies.txt"
