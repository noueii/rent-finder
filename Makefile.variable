.EXPORT_ALL_VARIABLES:
POSTGRES_CONTAINER_NAME = rent-finder-db
DB_NAME = rent-finder
DB_DRIVER = postgres
DB_USER = root
DB_USER_PASSWORD = rentsecret
DB_HOST = localhost
DB_INTERNAL_PORT = 5432
DB_EXTERNAL_PORT = 5432
DB_URL := ${DB_DRIVER}://${DB_USER}:${DB_USER_PASSWORD}@${DB_HOST}:${DB_EXTERNAL_PORT}/${DB_NAME}
POSTGRES_URL = ${DB_DRIVER}://${DB_USER}:${DB_USER_PASSWORD}@${DB_HOST}:${DB_EXTERNAL_PORT}

TOOLS_DIR := tools
TOOLS_FILE := $(TOOLS_DIR)/tools.go
